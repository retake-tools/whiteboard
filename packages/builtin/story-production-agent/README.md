# Retake Story Production Agent

A bounded AgentPreset for coordinating the built-in story-production target.

This directory is the declarative source for `retake.package.story-production-agent@0.3.0`.
